/** User-facing copy when Hermes/oracle data cannot be loaded (paired with `live_snapshot_unavailable`). */
export const LIVE_ORACLE_UNAVAILABLE_MESSAGE =
  "Live oracle data is temporarily unavailable.";
