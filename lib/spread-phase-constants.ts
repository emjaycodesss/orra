/** Deal grid — sync with `.spread-phase__table` and spread audio. */
export const SPREAD_PHASE_CARD_COUNT = 21;

/** Must match `.spread-phase__card` `animation-delay` in globals.css */
export const SPREAD_DEAL_STAGGER_MS = 120;

/** Must match `reading-spread-deal` on `.spread-phase__card` in globals.css */
export const SPREAD_DEAL_ANIMATION_MS = 500;

/** Must match `.spread-phase--reduced` deal duration in globals.css */
export const SPREAD_DEAL_ANIMATION_REDUCED_MS = 70;
